/**
 * Public surface of the app's notification chime: the bundled sound and the
 * one call that plays it, throttled so a burst of finishing agents is one chime.
 */
export { playNotificationSound } from './player';
