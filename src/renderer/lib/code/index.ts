/**
 * Public surface for code-rendering helpers: the shared Shiki highlighter and
 * the gutter measurement every code surface sizes its line numbers with.
 */
export { codeGutterDigits } from './gutter';
export { highlightCode } from './highlighter';
