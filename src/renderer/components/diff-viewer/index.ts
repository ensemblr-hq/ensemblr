/**
 * Public surface for the diff-viewer feature: a single rich single-file diff
 * component and the comment shape it renders inline. Import from
 * `@/renderer/components/diff-viewer` outside this folder.
 */
export type { DiffComment } from './diff-comment-thread';
export { DiffViewer } from './diff-viewer';
