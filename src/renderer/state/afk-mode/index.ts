/**
 * Public surface of the renderer's AFK state: the sync effect the app root
 * installs to mirror a spawn-time activation main made on its behalf.
 */
export { useAfkModeSync } from './afk-mode-sync.ts';
