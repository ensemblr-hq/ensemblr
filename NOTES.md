## Ensemblr v0.1.0-beta.20

**The shell moves to Electron 44, and with it the macOS floor rises from Monterey to Ventura.** A Mac on macOS 12 cannot run this build; the Homebrew cask declares the new floor, so `brew` refuses rather than installing something that will not open, and `0.1.0-beta.19` stays the last release for those machines. Linux is unaffected — the AppImage carries its own runtime. Everything else here is interaction repair: dragging the right sidebar no longer relays out the whole workspace, the drawn menu bar's focus-moving rows work, and a Linux user with no `git` on PATH is finally offered advice that applies to their machine.

### What's Changed

#### Changed

* **Electron 43 → 44**: the embedded Node stays at 24.18.1, so `engines`, `.nvmrc` and the `@types/node` `^24` pin are all still correct, but two things move with it. The **macOS floor** rises from Monterey to Ventura, per the `44-x-y` branch README — it is declared outside this repository, in the `ensemblr-hq/homebrew-tap` cask, which is why the stanza is bumped there rather than here. And the **clipboard module** is rearchitected against the W3C Clipboard API: `clipboard.writeText` returns a Promise instead of writing synchronously, and the module is gone from the renderer entirely. Both constraints are now written into `.claude/rules/stack.md` rather than rediscovered.

#### Fixes

* **Dragging the right sidebar relaid out the entire workspace on every frame**: the drag wrote `rightSidebarSizePercentAtom` sixty times a second, and that atom feeds an unmemoized `WorkbenchLayoutProvider` value, so each frame re-rendered the whole workspace subtree — while the new width also reached `ResizablePanel` as a changed `defaultSize`, which react-resizable-panels lists in its registration effect, so every frame unregistered the panel and relaid out the group as well. The width commit is now debounced to once per settled drag, the panel's `defaultSize` is frozen at mount by the controller that already owns clamping and the collapsed flag, and the controller no longer subscribes to the width atom at all — it writes with `useSetAtom` and reads the persisted value once through the store, so even the settled commit re-renders nothing. The pending width is flushed on unmount *and* on `pagehide`, because quitting tears the renderer down without unmounting React. The queued follow-up rows sliding to places they were never dragged to had the same cause: a new `useResizingWidth` reports while the list's box is still moving, and each row takes a zero-duration layout transition so it lands in its reflowed place outright while `Reorder.Group` can still measure it. (#385)

* **The drawn menu bar's focus-moving rows did nothing, and Reload was permanently dead** (THE-207): invoking a row is a renderer→main→renderer round trip, and `AppMenuBar` performed the focus restore from `onSelect` while the menu still owned focus — Radix's `onCloseAutoFocus` then refocused the trigger from a `setTimeout(0)` in FocusScope's unmount cleanup, so whichever resolved last won. The row is now held from `onSelect` and reported from `onCloseAutoFocus`, with the trigger restore `preventDefault()`ed and focus put back on the element the bar was opened over; focus already inside the bar is never recorded as an origin, so a reopen after a dismissal does not hand the row back the trigger. Reload was dead for an unrelated reason: it carries a `click` closure rather than a `role`, so it does not claim Ctrl+R — and a closure is opaque to the serializer that builds the drawn bar. A new `drawnRole` says what such a row is without handing Electron the role's accelerator. (#384)

* **A Linux user with no `git` on PATH got macOS-only advice** (THE-206): the `git-executable` setup check is blocking, and it built its whole failure output unconditionally — the detail sentence named the Xcode Command Line Tools, the button ran `xcode-select --install`, and the documentation link pointed at the git-scm macOS page. `getGitExecutableCheck` now takes the platform the diagnostics service already resolves to gate `secret-storage`, and both the actions and the message branch on it: darwin keeps install-command-line-tools, the mac docs page and retry, while every other platform gets the docs page and retry, pointed at git-scm/download/linux on Linux and git-scm/downloads when unmapped. The new `git-not-found-macos` code inherits the existing sentence and its `ru`/`el` translations verbatim; `git-not-found` keeps its name, becomes platform-neutral, and had its two translations reworded. (#383)

* **"Copy path" reported success before the clipboard had been written**: Electron 44's `clipboard.writeText` returns a Promise, and the `copy-path` open target left it unawaited — so the success toast raced the write, and a failure surfaced as an unhandled rejection rather than an error the user could see.

#### Dependencies

* Electron `43.4.0` → `44.0.0`, Biome `2.5.9` → `2.5.10` (with the matching `$schema` pin in `biome.json`), Vite `8.2.1` → `8.2.2`, Vitest `4.1.10` → `4.1.11`, i18next-cli `1.70.0` → `1.71.3`, `@anthropic-ai/claude-agent-sdk` `0.3.223` → `0.3.251`, streamdown `2.5.0` → `2.6.0`, lucide-react `1.31.0` → `1.34.0`, motion `13.1.0` → `13.1.1`, shadcn `4.18.0` → `4.19.0`, plus the smaller `@tanstack`, `@testing-library`, `happy-dom` and `@iconify-json` bumps. i18next-cli 1.71.3 pulls in `@swc/core`, whose postinstall only probes for a native binding that is already installed, so it is denied in `allowScripts` alongside `core-js-pure` and `node-pty`.

---

*Full changelog*: https://github.com/ensemblr-hq/ensemblr/compare/v0.1.0-beta.19...v0.1.0-beta.20
