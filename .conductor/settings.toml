"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run = "./scripts/with-pinned-node.sh npm run dev"
run_mode = "concurrent"
setup = "./scripts/with-pinned-node.sh npm ci --allow-remote=all"
